import 'package:flutter/material.dart';

class AppColors {
  // =============================
  // LIGHT THEME COLORS
  // =============================

  static const Color lightBackground = Color(0xFFFFFFFF); // --background
  static const Color lightForeground = Color(0xFF030213); // --foreground

  static const Color lightCard = Color(0xFFFFFFFF); // --card
  static const Color lightMuted = Color(0xFFECECF0); // --muted
  static const Color lightMutedForeground = Color(0xFF717182);

  static const Color lightPrimary = Color(0xFF030213); // --primary
  static const Color lightPrimaryForeground = Color(0xFFFFFFFF);

  static const Color lightSecondary = Color(0xFFF2F2F7); // approx secondary
  static const Color lightAccent = Color(0xFFE9EBEF); // --accent

  static const Color lightBorder = Color(0x1A000000); // rgba(0,0,0,0.1)
  static const Color lightInputBackground = Color(0xFFF3F3F5);

  static const Color destructive = Color(0xFFD4183D);

  // =============================
  // DARK THEME COLORS
  // =============================

  static const Color darkBackground = Color(0xFF0F0F10); // oklch(0.145)
  static const Color darkForeground = Color(0xFFF9FAFB); // oklch(0.985)

  static const Color darkCard = Color(0xFF0F0F10);
  static const Color darkMuted = Color(0xFF262626);
  static const Color darkMutedForeground = Color(0xFF9CA3AF);

  static const Color darkPrimary = Color(0xFFF9FAFB);
  static const Color darkPrimaryForeground = Color(0xFF1F2933);

  static const Color darkSecondary = Color(0xFF262626);
  static const Color darkAccent = Color(0xFF262626);

  static const Color darkBorder = Color(0xFF262626);

  // =============================
  // COMMON
  // =============================

  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFFACC15);
  static const Color info = Color(0xFF38BDF8);
}
